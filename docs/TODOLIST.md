1.setting中ResultCard的各种UI风格选择(牛皮纸、玻璃拟态、终端风格、粘土拟态等)

2.接入deepseek API 生成 英文解释、词源等简练信息 -> 软件启动要检测用户在设置中填写的API是否有效，若有效才启动AI辅助功能，否则关闭，只使用字典资源。对于开启AI辅助功能后，阶段为：首先显示出字典提供的单词 + 发音 + 词性 + 中文翻译，然后调用API通过JSON的形式将单词发出，要求模型输出该单词的英文释义 以及词根及词源，在等待模型输出的过程中，若网络延迟或者模型问题导致时间较长，应在ResultCard的底部显示一个...形式的加载动画，直到接受到模型完整输出然后以verbose的形式显示到resultcard上，若获取失败应在底部提醒。注意：deekseep返回的内容应该及其精炼，每项输出应该小于30词

3.加强tesseract的识别能力

4.识别单词后产生的所有情况【成功、识别失败、没有这个单词等等】 -> 规范好显示在ResultCard上的内容，比如如果没有对应单词则只显示UNKNOWN

5.通过TDD测试驱动开发  -> 加入单元/集成测试尽可能覆盖所有代码的所有情况。

6.【手工测试】测试实际游戏/网页过程中，软件的可行度 

​	解决在游戏开启全屏时以及按键冲突等问题

7.通过windeployqt + innosetup 完成依赖打包和安装过程可视化 -> 整理成SKILL(目前先只考虑windows，后续跨平台linux)

8.性能优化 + 内存泄露 + 野指针 + 各实例生命周期可优雅结束   -> 使用工具进行检测

9.开机自启功能

10.查询历史功能

11.跨平台

12.在头文件中所有变量及函数加入中文doxygen风格注释，格式为：

```c++
变量：
	///< 
函数[brief应简洁，小于50字]：
    /**
     * @brief
     * @param [in]
	 * @param [out]
     */
```

​		